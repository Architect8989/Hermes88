# ─────────────────────────────────────────────────────────────────────────────
# Rhodawk AI — Hermes88 requirements.txt  v5.0 (Peak Architecture)
# All Python runtime dependencies for the full multi-agent swarm.
# Install: pip3 install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# ── Agent 1: hermes-agent (orchestration + Telegram gateway) ─────────────────
# hermes-agent is NOT on PyPI — installed directly in the Dockerfile with a
# graceful fallback chain. Do NOT list it here; a hard requirement causes
# `pip install -r` to fail unconditionally when the package is absent.

# ── MCP (Model Context Protocol) client ──────────────────────────────────────
mcp>=1.0.0

# ── gRPC stack (openclaude gRPC client / server stubs) ───────────────────────
grpcio>=1.62.0
grpcio-tools>=1.62.0
protobuf>=4.25.0

# ── OpenAI-compatible client ──────────────────────────────────────────────────
openai>=1.0.0

# ── HTTP clients ──────────────────────────────────────────────────────────────
requests>=2.31.0
aiohttp>=3.9.0
aiofiles>=23.2.0
httpx>=0.27.0

# ── Telegram bot SDK ──────────────────────────────────────────────────────────
python-telegram-bot>=20.7

# ── HuggingFace integration ───────────────────────────────────────────────────
huggingface_hub>=0.21.0

# ── Media / document ingestion ────────────────────────────────────────────────
PyMuPDF>=1.23.0
pdfminer.six>=20221105
pillow>=10.0.0

# ── Web search (zero-API-key fallback) ────────────────────────────────────────
duckduckgo-search>=6.0.0

# ── Utilities ─────────────────────────────────────────────────────────────────
setuptools>=69.0.0
wheel>=0.43.0

# ─────────────────────────────────────────────────────────────────────────────
# PEAK ARCHITECTURE DEPENDENCIES
# ─────────────────────────────────────────────────────────────────────────────

# ── Memory Engine (vector search, importance scoring, embeddings) ─────────────
mem0ai>=0.1.0
chromadb>=0.4.0
sentence-transformers>=2.2.0
numpy>=1.24.0

# ── Orchestration (multi-model routing, unified LLM interface) ────────────────
litellm>=1.30.0
pydantic-ai>=0.0.1
pydantic>=2.0.0

# ── Task Queue (background execution, scheduling, Redis-backed) ──────────────
dramatiq[redis]>=1.14.0
apscheduler>=3.10.0
celery[redis]>=5.3.0

# ── Web Framework (health endpoint, internal APIs) ────────────────────────────
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# ── Communication (multi-channel messaging) ──────────────────────────────────
discord.py>=2.3.0
slack-sdk>=3.27.0

# ── Financial (Stripe integration for payments + subscriptions) ──────────────
stripe>=8.0.0

# ── Calendar (Google Calendar + scheduling) ──────────────────────────────────
google-api-python-client>=2.100.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0

# ── RSS / Content (feed monitoring, frontmatter parsing) ─────────────────────
feedparser>=6.0.0
python-frontmatter>=1.1.0

# ── Document Generation (pandoc wrapper for PDF/DOCX/HTML export) ─────────────
pypandoc>=1.12.0

# ── Voice (speech-to-text transcription) ─────────────────────────────────────
openai-whisper>=20231117

# ── Browser Automation (programmatic web interaction) ─────────────────────────
browser-use>=0.1.0
playwright>=1.40.0

# ── Security (static analysis, dependency scanning) ──────────────────────────
bandit>=1.7.0
safety>=3.0.0

# ── Data (Redis client with hiredis, SQLite utilities) ────────────────────────
redis[hiredis]>=5.0.0
sqlite-utils>=3.35.0

# ── Async / Event Loop ────────────────────────────────────────────────────────
asyncio-mqtt>=0.16.0
watchfiles>=0.21.0
