> **DEPRECATED** — This file is the v1 hardcoded pipeline, kept for backwards compatibility.
> The active skill is `SKILL.md` (v2 — Rhodawk Autonomous Architect, dynamic format).
> Hermes loads both; SKILL.md takes precedence for all new task routing.

---

# DevOps Pipeline v1 — Bug Fixer (Legacy)

This skill is superseded by `SKILL.md`. It handled the original fixed bug-fix pipeline.
New tasks (scaffolding, media ingestion, cross-checking, resilient push) are handled by `SKILL.md`.

For reference, the original 7-step pipeline (clone → ripgrep → openclaude 3x → JCode swarm → commit → push → report)
is now embedded inside `SKILL.md` as part of the broader Autonomous Architect procedure.

See `SKILL.md` for the current implementation.
